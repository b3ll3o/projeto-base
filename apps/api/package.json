{
  "name": "@projeto/api",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "lint": "echo 'apps/api lint stub (real wiring in Phase 3)' && exit 0",
    "typecheck": "tsc --noEmit",
    "test": "echo 'apps/api test stub (real wiring in Phase 3)' && exit 0",
    "build": "echo 'apps/api build stub (real wiring in Phase 3)' && exit 0"
  },
  "devDependencies": {
    "@projeto/eslint-config": "workspace:*",
    "@projeto/tsconfig": "workspace:*",
    "@types/node": "^20.16.0",
    "eslint": "^9.12.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
